# Quanty.studio Security Architecture Diagrams

This document provides visual representations of the multi-tenant security architecture.

---

## 1. Multi-Tenant Data Isolation Model

```
┌────────────────────────────────────────────────────────────────────────┐
│                          QUANTY.STUDIO DATABASE                         │
│                     (Single PostgreSQL Instance)                        │
└────────────────────────────────────────────────────────────────────────┘

         ┌──────────────────────────────────────────────────┐
         │             USERS TABLE                          │
         │  id  │  email  │  password_hash  │  full_name    │
         │──────┼─────────┼─────────────────┼───────────────│
         │ u1   │ alice@  │ $2b$12$...     │ Alice         │
         │ u2   │ bob@    │ $2b$12$...     │ Bob           │
         │ u3   │ carol@  │ $2b$12$...     │ Carol         │
         └──────────────────────────────────────────────────┘
                  │            │            │
                  ├────────────┼────────────┤
                  ▼            ▼            ▼
         ┌────────────────────────────────────────────────────┐
         │           WORKSPACES TABLE                          │
         │  id   │  name        │  type     │  owner_id       │
         │───────┼──────────────┼───────────┼─────────────────│
         │ ws1   │ Alice Work   │ personal  │ u1              │
         │ ws2   │ Team Alpha   │ team      │ u1              │
         │ ws3   │ Bob Work     │ personal  │ u2              │
         │ ws4   │ Carol Work   │ personal  │ u3              │
         └────────────────────────────────────────────────────┘
                  │            │            │
                  └────────────┼────────────┘
                               ▼
         ┌──────────────────────────────────────────────────────────┐
         │         WORKSPACE_MEMBERS TABLE (Junction)               │
         │  workspace_id  │  user_id  │  role                       │
         │────────────────┼───────────┼─────────────────────────────│
         │  ws1           │  u1       │  owner     ← Alice's personal│
         │  ws2           │  u1       │  owner     ← Alice owns Team │
         │  ws2           │  u2       │  editor    ← Bob is editor  │
         │  ws2           │  u3       │  viewer    ← Carol is viewer│
         │  ws3           │  u2       │  owner     ← Bob's personal │
         │  ws4           │  u3       │  owner     ← Carol's personal│
         └──────────────────────────────────────────────────────────┘
                               │
                               ▼
         ┌──────────────────────────────────────────────────────────┐
         │            TABLES_METADATA TABLE                          │
         │  id   │  workspace_id  │  name        │  created_by      │
         │───────┼────────────────┼──────────────┼──────────────────│
         │  t1   │  ws1           │  sales_data  │  u1              │
         │  t2   │  ws2           │  team_sales  │  u1              │
         │  t3   │  ws3           │  bob_data    │  u2              │
         │  t4   │  ws4           │  carol_data  │  u3              │
         └──────────────────────────────────────────────────────────┘

         🔒 RLS ENFORCEMENT RULES:
         ─────────────────────────
         • Alice (u1) can see: t1, t2  ✓  (member of ws1, ws2)
         • Alice (u1) CANNOT see: t3, t4  ✗  (not member of ws3, ws4)

         • Bob (u2) can see: t2, t3  ✓  (member of ws2, ws3)
         • Bob (u2) CANNOT see: t1, t4  ✗  (not member of ws1, ws4)

         • Carol (u3) can see: t2, t4  ✓  (member of ws2, ws4)
         • Carol (u3) CANNOT see: t1, t3  ✗  (not member of ws1, ws3)
```

---

## 2. Request Flow with Security Layers

```
┌─────────────────────────────────────────────────────────────────────┐
│  CLIENT: Web Browser / Mobile App                                   │
│  User: alice@example.com                                            │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP Request
                              │ GET /api/workspaces/ws2/tables
                              │ Authorization: Bearer eyJhbGc...
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  LAYER 1: API GATEWAY / REVERSE PROXY                               │
│  • HTTPS termination                                                │
│  • Rate limiting (100 req/min)                                      │
│  • DDoS protection                                                  │
└─────────────────────────────────────────────────────────────────────┘
                              │ ✓ PASS
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  LAYER 2: FASTAPI MIDDLEWARE                                        │
│  • CORS validation                                                  │
│  • Security headers injection                                       │
│  • Request logging                                                  │
└─────────────────────────────────────────────────────────────────────┘
                              │ ✓ PASS
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  LAYER 3: JWT AUTHENTICATION                                        │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 1. Extract Bearer token from header                         │   │
│  │ 2. Decode JWT: { "sub": "u1", "exp": 1735171200 }          │   │
│  │ 3. Verify signature with JWT_SECRET_KEY                     │   │
│  │ 4. Check expiration (exp > now())                           │   │
│  │ 5. Extract user_id = "u1"                                   │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              │ ✓ AUTHENTICATED: user_id = u1
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  LAYER 4: WORKSPACE AUTHORIZATION                                   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ require_workspace_access(workspace_id = "ws2")              │   │
│  │                                                              │   │
│  │ Query: SELECT role FROM workspace_members                   │   │
│  │        WHERE workspace_id = 'ws2' AND user_id = 'u1'        │   │
│  │                                                              │   │
│  │ Result: role = 'owner'  ✓                                   │   │
│  │                                                              │   │
│  │ WorkspaceContext:                                            │   │
│  │   - workspace_id: ws2                                        │   │
│  │   - user_id: u1                                              │   │
│  │   - role: owner                                              │   │
│  │   - can_edit: true                                           │   │
│  │   - can_delete: true                                         │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              │ ✓ AUTHORIZED: owner of ws2
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  LAYER 5: RLS CONTEXT SETUP                                         │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ set_rls_context(db, user_id = "u1")                         │   │
│  │                                                              │   │
│  │ SQL: SET LOCAL app.current_user_id = 'u1'                   │   │
│  │                                                              │   │
│  │ PostgreSQL Session Variable Set ✓                           │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              │ ✓ RLS CONTEXT SET
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  LAYER 6: DATABASE QUERY EXECUTION                                  │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ Application Query:                                           │   │
│  │ SELECT * FROM tables_metadata WHERE workspace_id = :ws_id   │   │
│  │                                                              │   │
│  │ PostgreSQL RLS Automatically Transforms To:                 │   │
│  │ SELECT * FROM tables_metadata                               │   │
│  │ WHERE workspace_id = :ws_id                                 │   │
│  │   AND is_workspace_member(workspace_id, current_user_id())  │   │
│  │       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^      │   │
│  │       RLS Policy Auto-Added                                 │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ ✓ QUERY FILTERED
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  LAYER 7: RLS POLICY EVALUATION                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ is_workspace_member(ws2, u1) = ?                            │   │
│  │                                                              │   │
│  │ SELECT 1 FROM workspace_members                             │   │
│  │ WHERE workspace_id = 'ws2' AND user_id = 'u1'               │   │
│  │                                                              │   │
│  │ Result: 1 row found → TRUE ✓                                │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              │ ✓ RLS POLICY PASSED
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  RESULT: FILTERED DATA                                              │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ Returns only tables from workspace ws2:                     │   │
│  │                                                              │   │
│  │ [                                                            │   │
│  │   {                                                          │   │
│  │     "id": "t2",                                              │   │
│  │     "name": "team_sales",                                    │   │
│  │     "workspace_id": "ws2",                                   │   │
│  │     "created_by": "u1"                                       │   │
│  │   }                                                          │   │
│  │ ]                                                            │   │
│  │                                                              │   │
│  │ ✓ Tables from ws1, ws3, ws4 are NOT included                │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ JSON Response
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  CLIENT RECEIVES SECURE DATA                                        │
│  • Only sees data from authorized workspace                         │
│  • Cannot access other workspaces                                   │
│  • RLS enforced at database level (cannot bypass)                   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 3. Attack Scenario: Unauthorized Access Attempt

```
┌─────────────────────────────────────────────────────────────────────┐
│  ATTACKER: Malicious User (Bob)                                     │
│  Tries to access Alice's personal workspace                         │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP Request
                              │ GET /api/workspaces/ws1/tables
                              │ Authorization: Bearer <bob_token>
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  LAYER 3: JWT AUTHENTICATION                                        │
│  ✓ Valid token → user_id = u2 (Bob)                                │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  LAYER 4: WORKSPACE AUTHORIZATION                                   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ require_workspace_access(workspace_id = "ws1")              │   │
│  │                                                              │   │
│  │ Query: SELECT role FROM workspace_members                   │   │
│  │        WHERE workspace_id = 'ws1' AND user_id = 'u2'        │   │
│  │                                                              │   │
│  │ Result: NO ROWS ✗                                           │   │
│  │                                                              │   │
│  │ ❌ BLOCKED: User u2 is NOT a member of workspace ws1        │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ ✗ AUTHORIZATION FAILED
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  HTTP RESPONSE: 403 Forbidden                                       │
│  {                                                                   │
│    "detail": "Access denied: You are not a member of this workspace"│
│  }                                                                   │
│                                                                      │
│  🛡️ SECURITY GUARANTEE:                                             │
│  Bob CANNOT access Alice's workspace at application layer           │
└─────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│  WHAT IF ATTACKER BYPASSES APPLICATION LAYER?                       │
│  (e.g., SQL injection or compromised application code)              │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ Direct SQL Query
                              │ (bypassing application checks)
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  LAYER 5: RLS CONTEXT (Still Set)                                   │
│  SET LOCAL app.current_user_id = 'u2'  (Bob)                       │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  LAYER 6: MALICIOUS QUERY                                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ Attacker's Query:                                            │   │
│  │ SELECT * FROM tables_metadata WHERE workspace_id = 'ws1'    │   │
│  │                                                              │   │
│  │ PostgreSQL RLS STILL Transforms To:                         │   │
│  │ SELECT * FROM tables_metadata                               │   │
│  │ WHERE workspace_id = 'ws1'                                  │   │
│  │   AND is_workspace_member('ws1', current_user_id())         │   │
│  │                                   ^^^^^^^^^^^^^^^^          │   │
│  │                                   = 'u2' (Bob)              │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  LAYER 7: RLS POLICY EVALUATION                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ is_workspace_member('ws1', 'u2') = ?                        │   │
│  │                                                              │   │
│  │ SELECT 1 FROM workspace_members                             │   │
│  │ WHERE workspace_id = 'ws1' AND user_id = 'u2'               │   │
│  │                                                              │   │
│  │ Result: NO ROWS → FALSE ✗                                   │   │
│  │                                                              │   │
│  │ ❌ RLS POLICY BLOCKS ACCESS                                  │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ ✗ RLS POLICY FAILED
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  RESULT: EMPTY SET                                                  │
│  Returns: []  (no rows)                                             │
│                                                                      │
│  🛡️ DEFENSE IN DEPTH:                                               │
│  Even with compromised application, RLS prevents data leakage       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 4. Role-Based Access Control Matrix

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    WORKSPACE PERMISSIONS MATRIX                            │
└────────────────────────────────────────────────────────────────────────────┘

OPERATION                    │ VIEWER  │ EDITOR  │ OWNER
─────────────────────────────┼─────────┼─────────┼────────
View Workspace               │   ✓     │   ✓     │   ✓
View Members                 │   ✓     │   ✓     │   ✓
View Tables                  │   ✓     │   ✓     │   ✓
View Table Data (SELECT)     │   ✓     │   ✓     │   ✓
View Dashboards              │   ✓     │   ✓     │   ✓
View Query History           │   ✓     │   ✓     │   ✓
─────────────────────────────┼─────────┼─────────┼────────
Create Table                 │   ✗     │   ✓     │   ✓
Update Table Metadata        │   ✗     │   ✓     │   ✓
Insert Data (INSERT)         │   ✗     │   ✓     │   ✓
Update Data (UPDATE)         │   ✗     │   ✓     │   ✓
Create Dashboard             │   ✗     │   ✓     │   ✓
Update Dashboard             │   ✗     │   ✓     │   ✓
─────────────────────────────┼─────────┼─────────┼────────
Delete Table                 │   ✗     │   ✗     │   ✓
Delete Data (DELETE)         │   ✗     │   ✗     │   ✓
Delete Dashboard             │   ✗     │   ✗     │   ✓
Invite Members               │   ✗     │   ✗     │   ✓
Remove Members               │   ✗     │   ✗     │   ✓
Change Member Roles          │   ✗     │   ✗     │   ✓
Update Workspace Settings    │   ✗     │   ✗     │   ✓
Delete Workspace             │   ✗     │   ✗     │   ✓*

* Cannot delete personal workspace (protected by RLS)


┌────────────────────────────────────────────────────────────────────────────┐
│                    RLS POLICY ENFORCEMENT                                  │
└────────────────────────────────────────────────────────────────────────────┘

TABLE: tables_metadata
──────────────────────

POLICY: tables_select
┌────────────────────────────────────────────────────────┐
│ FOR SELECT USING (                                     │
│     is_workspace_member(workspace_id, current_user_id())│
│ )                                                       │
│                                                         │
│ Effect: Viewers, Editors, Owners can SELECT ✓          │
└────────────────────────────────────────────────────────┘

POLICY: tables_insert
┌────────────────────────────────────────────────────────┐
│ FOR INSERT WITH CHECK (                                │
│     can_edit_workspace(workspace_id, current_user_id())│
│ )                                                       │
│                                                         │
│ Effect: Only Editors, Owners can INSERT ✓              │
│         Viewers blocked ✗                              │
└────────────────────────────────────────────────────────┘

POLICY: tables_update
┌────────────────────────────────────────────────────────┐
│ FOR UPDATE USING (                                     │
│     can_edit_workspace(workspace_id, current_user_id())│
│ )                                                       │
│                                                         │
│ Effect: Only Editors, Owners can UPDATE ✓              │
│         Viewers blocked ✗                              │
└────────────────────────────────────────────────────────┘

POLICY: tables_delete
┌────────────────────────────────────────────────────────┐
│ FOR DELETE USING (                                     │
│     is_workspace_owner(workspace_id, current_user_id())│
│ )                                                       │
│                                                         │
│ Effect: Only Owners can DELETE ✓                       │
│         Editors, Viewers blocked ✗                     │
└────────────────────────────────────────────────────────┘
```

---

## 5. Personal Workspace Protection

```
┌────────────────────────────────────────────────────────────────────────────┐
│              PERSONAL WORKSPACE DELETION PROTECTION                        │
└────────────────────────────────────────────────────────────────────────────┘

USER REGISTRATION FLOW:
───────────────────────

1. User signs up
   POST /api/auth/register
   { "email": "alice@example.com", "password": "..." }
        │
        ▼
2. Insert user into users table
   INSERT INTO users (email, password_hash, ...)
   VALUES ('alice@example.com', '$2b$12$...', ...)
        │
        ▼
3. TRIGGER: create_personal_workspace() ← AUTOMATIC
   ┌────────────────────────────────────────────────┐
   │ CREATE TRIGGER create_user_workspace           │
   │     AFTER INSERT ON users                      │
   │     FOR EACH ROW                               │
   │     EXECUTE FUNCTION create_personal_workspace();│
   └────────────────────────────────────────────────┘
        │
        ▼
4. Personal workspace created
   INSERT INTO workspaces
   (name, slug, type, owner_id)
   VALUES
   ('My Workspace', 'personal-<uuid>', 'personal', alice_id)
        │
        ▼
5. User added as owner
   INSERT INTO workspace_members
   (workspace_id, user_id, role)
   VALUES (personal_ws_id, alice_id, 'owner')


DELETION ATTEMPT:
─────────────────

Scenario: Alice tries to delete her personal workspace

   DELETE FROM workspaces WHERE id = :personal_ws_id
        │
        ▼
   RLS POLICY: workspace_delete
   ┌────────────────────────────────────────────────┐
   │ FOR DELETE USING (                             │
   │     is_workspace_owner(id, current_user_id())  │
   │     AND type != 'personal'  ← CRITICAL CHECK   │
   │ )                                               │
   └────────────────────────────────────────────────┘
        │
        │ Check: is_workspace_owner(ws_id, alice_id) = TRUE ✓
        │ Check: type = 'personal' ✗
        │
        │ Result: TRUE AND (type != 'personal')
        │       = TRUE AND FALSE
        │       = FALSE
        ▼
   ❌ DELETION BLOCKED BY RLS

   PostgreSQL returns: 0 rows affected

   🛡️ PROTECTION:
   Personal workspace CANNOT be deleted
   - Even by the owner
   - Even with superuser privileges (if RLS not bypassed)
   - Prevents accidental data loss
```

---

## 6. Database Schema Entity Relationship Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    QUANTY.STUDIO ERD                                    │
│                  (With Security Annotations)                            │
└─────────────────────────────────────────────────────────────────────────┘


┌──────────────────────┐
│       USERS          │
│──────────────────────│
│ 🔑 id (PK)           │◄──────────────────────────┐
│ 🔒 email (UNIQUE)    │                            │
│ 🔒 password_hash     │                            │
│    full_name         │                            │
│    avatar_url        │                            │
│    is_active         │                            │
│    created_at        │                            │
└──────────────────────┘                            │
         │                                          │
         │ 1                                        │
         │                                          │
         │ owns                                     │
         │                                          │
         │ N                                        │
         ▼                                          │
┌──────────────────────┐                            │
│    WORKSPACES        │                            │
│──────────────────────│                            │
│ 🔑 id (PK)           │                            │
│ 🔐 owner_id (FK)     │────────────────────────────┘
│    name              │
│    slug (UNIQUE)     │
│ 🛡️ type (ENUM)       │  ← 'personal' | 'team'
│    description       │
│    settings (JSONB)  │
│    created_at        │
│                      │
│ 🔒 RLS ENABLED       │  ← Row Level Security ON
└──────────────────────┘
         │
         │ 1
         │
         │ referenced by
         │
         │ N
         ▼
┌──────────────────────────────────┐
│    WORKSPACE_MEMBERS             │  ← JUNCTION TABLE
│──────────────────────────────────│
│ 🔑 id (PK)                       │
│ 🔐 workspace_id (FK)             │───┐
│ 🔐 user_id (FK)                  │   │
│ 🛡️ role (ENUM)                   │   │  ← 'owner'|'editor'|'viewer'
│    invited_by (FK)               │   │
│    joined_at                     │   │
│                                  │   │
│ 🔒 UNIQUE(workspace_id, user_id) │   │  ← No duplicate memberships
│ 🔒 RLS ENABLED                   │   │
└──────────────────────────────────┘   │
         ▲                              │
         │                              │
         │ N                            │ 1
         │                              │
         │ member of                    │
         │                              │
         │ 1                            │
┌──────────────────────┐               │
│       USERS          │               │
│ (same as above)      │               │
└──────────────────────┘               │
                                       │
                                       │
         ┌─────────────────────────────┘
         │
         │ 1
         │
         │ contains
         │
         │ N
         ▼
┌──────────────────────────────────┐
│    TABLES_METADATA               │
│──────────────────────────────────│
│ 🔑 id (PK)                       │
│ 🔐 workspace_id (FK) ← MANDATORY │  🛡️ ALL tables scoped by workspace
│    name                          │
│    display_name                  │
│    schema_definition (JSONB)     │
│    row_count                     │
│ 🔐 created_by (FK)               │
│    created_at                    │
│                                  │
│ 🔒 UNIQUE(workspace_id, name)    │  ← Table names unique per workspace
│ 🔒 RLS ENABLED                   │
└──────────────────────────────────┘
         │
         │ 1
         │
         │ referenced by
         │
         │ N
         ▼
┌──────────────────────────────────┐
│    QUERY_HISTORY                 │  ← AUDIT LOG
│──────────────────────────────────│
│ 🔑 id (PK)                       │
│ 🔐 workspace_id (FK)             │  🛡️ Workspace-scoped audit trail
│ 🔐 user_id (FK)                  │
│ 🔐 table_id (FK, nullable)       │
│    question (TEXT)               │  ← User's natural language query
│    sql_query (TEXT)              │  ← Generated SQL
│    result_summary (TEXT)         │
│    execution_time_ms             │
│    created_at                    │
│                                  │
│ 🔒 RLS ENABLED                   │
│ 🔒 IMMUTABLE (no UPDATE/DELETE)  │  ← Audit integrity
└──────────────────────────────────┘


┌──────────────────────────────────┐
│    DASHBOARDS                    │
│──────────────────────────────────│
│ 🔑 id (PK)                       │
│ 🔐 workspace_id (FK)             │  🛡️ Workspace-scoped dashboards
│    name                          │
│    description                   │
│    config (JSONB)                │  ← Widget configurations
│    is_public (BOOLEAN)           │  ← Future: public sharing
│ 🔐 created_by (FK)               │
│    created_at                    │
│                                  │
│ 🔒 RLS ENABLED                   │
└──────────────────────────────────┘


┌──────────────────────────────────┐
│    WORKSPACE_INVITATIONS         │
│──────────────────────────────────│
│ 🔑 id (PK)                       │
│ 🔐 workspace_id (FK)             │
│    email                         │
│ 🛡️ role (ENUM)                   │  ← Role for invitee
│    token (UNIQUE)                │  🔒 Secure random token
│ 🔐 invited_by (FK)               │
│    expires_at                    │  ← 7 days expiration
│    accepted_at                   │
│    created_at                    │
│                                  │
│ 🔒 RLS ENABLED                   │
└──────────────────────────────────┘


LEGEND:
───────
🔑 Primary Key
🔐 Foreign Key
🔒 Security Constraint
🛡️ Security-Critical Field
```

---

## 7. Security Validation Flowchart

```
START: API Request Received
   │
   ▼
┌────────────────────────┐
│ Has Authorization      │ NO
│ Header?                ├────► [401 Unauthorized]
└────────┬───────────────┘
         │ YES
         ▼
┌────────────────────────┐
│ JWT Token Valid?       │ NO
│ - Signature OK?        ├────► [401 Invalid Token]
│ - Not Expired?         │
└────────┬───────────────┘
         │ YES
         │ Extract user_id
         ▼
┌────────────────────────┐
│ Workspace ID in        │ NO
│ Request?               ├────► [Continue without workspace context]
└────────┬───────────────┘
         │ YES
         ▼
┌────────────────────────┐
│ User Member of         │ NO
│ Workspace?             ├────► [403 Not a Member]
│ (Check workspace_      │
│  members table)        │
└────────┬───────────────┘
         │ YES
         ▼
┌────────────────────────┐
│ Required Role Met?     │ NO
│ (e.g., need 'editor')  ├────► [403 Insufficient Role]
└────────┬───────────────┘
         │ YES
         ▼
┌────────────────────────┐
│ SET RLS Context        │
│ app.current_user_id    │
│ = user_id              │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ Execute Business       │
│ Logic                  │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ Database Query         │
│ (RLS Auto-Applies)     │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ RLS Policy Check       │
│ is_workspace_member()  │
└────────┬───────────────┘
         │ PASS
         ▼
┌────────────────────────┐
│ Return Filtered Data   │
│ (Only Authorized Rows) │
└────────┬───────────────┘
         │
         ▼
    [200 OK]
    Return JSON Response


SECURITY CHECKPOINTS:
─────────────────────
✓ Checkpoint 1: Authentication (JWT)
✓ Checkpoint 2: Authorization (Workspace Membership)
✓ Checkpoint 3: Role Validation (RBAC)
✓ Checkpoint 4: RLS Context Setup
✓ Checkpoint 5: Database RLS Policy Enforcement

Each checkpoint is INDEPENDENT and provides defense in depth.
```

---

## 8. Summary: Why This Architecture is Secure

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    SECURITY GUARANTEES                                  │
└─────────────────────────────────────────────────────────────────────────┘

1. ✅ CANNOT access data from unauthorized workspaces
   ─────────────────────────────────────────────────
   • Application checks workspace membership
   • RLS enforces at database level
   • Even compromised app code cannot leak data

2. ✅ CANNOT escalate privileges
   ──────────────────────────
   • Role hierarchy enforced in application
   • RLS policies enforce role requirements
   • Cannot UPDATE own role (RLS blocks)

3. ✅ CANNOT delete personal workspace
   ─────────────────────────────────
   • RLS DELETE policy checks type != 'personal'
   • Trigger auto-creates personal workspace
   • Prevents accidental data loss

4. ✅ CANNOT bypass security with SQL injection
   ───────────────────────────────────────────
   • Parameterized queries only
   • Input validation on all endpoints
   • RLS provides secondary defense

5. ✅ CANNOT see other users' query history
   ──────────────────────────────────────
   • Query history scoped by workspace_id
   • RLS filters based on workspace membership
   • Audit trail integrity maintained

6. ✅ SCALABLE with minimal overhead
   ────────────────────────────────
   • Single database (no per-tenant databases)
   • Index-backed RLS helper functions
   • ~5ms overhead per query (one index lookup)

7. ✅ TESTABLE security boundaries
   ───────────────────────────────
   • Unit tests for RLS policies
   • Integration tests for API endpoints
   • Automated security audit script
```

---

**Document Version**: 1.0
**Last Updated**: 2025-12-25
**Maintained By**: Quanty.studio Security Team
