# ========================================
# AI DATA ANALYST - ENVIRONMENT VARIABLES
# ========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# Google Gemini API Key
# Get yours at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Database Configuration
# For development, use SQLite:
# DATABASE_URL=sqlite:///./sales.db

# For production with Supabase PostgreSQL (RECOMMENDED):
# Get connection string from: Supabase Dashboard → Settings → Database → Connection String → URI
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# JWT Authentication Settings
# CRITICAL: Generate a secure secret key for production!
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY=generate-a-secure-random-key-with-openssl-rand-hex-32
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# CORS Settings
# SECURITY: Comma-separated list of allowed origins
# Development:
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Production (replace with your actual domains):
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Email Configuration
# Choose provider: console (dev), sendgrid, ses, smtp
EMAIL_PROVIDER=console
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=AI Data Analyst

# SendGrid (optional - if EMAIL_PROVIDER=sendgrid)
# Get API key from: https://sendgrid.com
# SENDGRID_API_KEY=your-sendgrid-api-key

# AWS SES (optional - if EMAIL_PROVIDER=ses)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key

# SMTP (optional - if EMAIL_PROVIDER=smtp)
# Generic SMTP settings for Gmail, Outlook, etc.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_USE_TLS=true

# Backup Configuration
BACKUP_ENABLED=true
BACKUP_SCHEDULE=daily
BACKUP_RETENTION_DAYS=7

# ========================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ========================================
# Before deploying to production, ensure:
#
# ✅ Generate new JWT_SECRET_KEY with: openssl rand -hex 32
# ✅ Set DATABASE_URL to PostgreSQL (not SQLite)
# ✅ Update ALLOWED_ORIGINS to your actual domain(s)
# ✅ Never use wildcard "*" in ALLOWED_ORIGINS
# ✅ Configure EMAIL_PROVIDER (sendgrid, ses, or smtp)
# ✅ Enable HTTPS on your web server
# ✅ Set up database backups (BACKUP_ENABLED=true)
# ✅ Configure monitoring and logging
# ✅ Review and test multi-tenant isolation
# ✅ Test workspace invitation emails
# ✅ Set up audit log retention policy
